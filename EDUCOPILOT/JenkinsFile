pipeline {
    agent any

    stages {

        stage('Build and Deploy') {
            steps {
                sh 'chmod +x build.sh'
                sh './build.sh'
            }
        }

    }
}
